{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://codex.example/schemas/project.schema.json",
  "title": "CODEX Project",
  "type": "object",
  "required": ["codexVersion", "id", "title", "objects"],
  "properties": {
    "codexVersion": {"type": "string", "minLength": 1},
    "id": {"type": "string", "pattern": "^[A-Z][A-Z0-9]{1,15}-[0-9]{4,}$"},
    "title": {"type": "string", "minLength": 1},
    "profile": {"type": "string"},
    "objects": {
      "type": "array",
      "items": {"$ref": "#/$defs/object"}
    }
  },
  "additionalProperties": false,
  "$defs": {
    "relation": {
      "type": "object",
      "required": ["type", "target"],
      "properties": {
        "type": {"type": "string"},
        "target": {"type": "string"}
      },
      "additionalProperties": false
    },
    "object": {
      "type": "object",
      "required": ["id", "type", "title", "version", "status"],
      "properties": {
        "id": {"type": "string", "pattern": "^[A-Z][A-Z0-9]{1,15}-[0-9]{4,}$"},
        "type": {"type": "string"},
        "title": {"type": "string", "minLength": 1},
        "version": {"type": "string", "minLength": 1},
        "status": {"enum": ["draft", "review", "approved", "published", "deprecated", "archived"]},
        "language": {"type": "string"},
        "derivedFrom": {"type": "array", "items": {"type": "string"}},
        "relations": {"type": "array", "items": {"$ref": "#/$defs/relation"}},
        "metadata": {"type": "object"}
      },
      "additionalProperties": false
    }
  }
}
